# jank-nrepl-server

***WORK-IN-PROGRESS !!!***

(Now just only simple socket communication is available. nREPL protocol is WIP.)

Tested on MSYS2 Windows.

## dev

```bash
$ ./build-cpp.sh
$ lein run
```